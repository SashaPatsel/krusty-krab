<h1 id="krab-dreams">...Where Dreams Come True</h1>

<h2>Uneaten Patties!</h2>

<ul>
  {{#each krabby}}
    {{#unless devoured}}
      {{> patty-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>krabby that are devoured!</h2>

<ul>
  {{#each krabby}}
    {{#if devoured}}
      {{> patty-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a patty</h2>
<form class="create-form">

  <div class="form-group">
    <label for="krabby">Krabby Patty:</label>
    <input type="text" id="krabby" name="name">
  </div>



  <button type="submit">Add Krabby Patty</button>
</form>
