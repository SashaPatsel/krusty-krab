<h1>Krabby Patties</h1>

<h2>Uneaten Patties!</h2>

<ul>
  {{#each krabby}}
    {{#unless devoured}}
      {{> krabby/patty-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>krabby that are devoured!</h2>

<ul>
  {{#each krabby}}
    {{#if devoured}}
      {{> krabby/patty-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a patty</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cat Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Awake
  </div>

  <button type="submit">Add Krabby Patty</button>
</form>
